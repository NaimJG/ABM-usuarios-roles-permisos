<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>Roles</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <h1>Roles</h1>
  <a href="/">← Volver al inicio</a>

  <table border="1" cellpadding="5">
    <thead>
      <tr>
        <th>Nombre</th>
        <th>Permisos</th>
        <th>Acciones</th>
      </tr>
    </thead>
    <tbody>
      <% roles.forEach(rol => { %>
        <tr>
          <td><%= rol.nombre %></td>
          <td>
            <% if (rol.permisos.length > 0) { %>
              <%= rol.permisos.map(p => p.nombre).join(", ") %>
            <% } else { %>
              Sin permisos
            <% } %>
          </td>
          <td>
            <a href="/roles/<%= rol._id %>/edit">Editar permisos</a>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</body>
</html>